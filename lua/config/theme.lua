-- ============================================================
-- 主题选择
-- 切换主题：修改 active 后重启 Neovim 生效
-- ============================================================

return {
	-- "dracula" - dracula 主题（默认）
	-- "dank"    - dankcolors（matugen 动态生成的 base16）
	active = "dracula",
}
